import HeaderSaved from "../headers_footers/HeaderSaved";

const Saved = () => {
   return (
      <div className="saved-react">

         <HeaderSaved />

         
      </div>
   );
}

export default Saved;