// JS ARCHITECTURE
// Posts and Tells
// 1. both use useReducer hooks and i have the 2 reducer funtions respectively that handles every action that has to do with posts and tells
// location: 
// reducer functions are in the app.js component
// useReducer hooks are created in PostFeed, TellsFeed, UserProfile components
// dispatch functions are passed down into PostList and TellsList: where a function "toggle" takes arguments and determines which type of event the user wants to take on a post or tell