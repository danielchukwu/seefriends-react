import { useEffect } from "react";
import useVariables from "../customhooks/useVariables";

const Follow = (which, id) => {
   const {users_host_url, access_token} = useVariables();

   

   return (
      <div>Hi</div>
   );
}

export default Follow;